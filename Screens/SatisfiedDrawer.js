import { View, Text } from 'react-native'
import { SafeAreaView } from 'react-native-safe-area-context'
import React from 'react'

const SatisfiedDrawer = ({navigation}) => {
  return (
    <SafeAreaView>
    <View>
      <Text>satisfied drawer</Text>
    </View>
    </SafeAreaView>
  )
}

export default SatisfiedDrawer